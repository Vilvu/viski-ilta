# WhiskyApp — Development Task List

## Phase 0: Project Setup and Infrastructure

- [x] Initialize Git repository and create `.gitignore`
- [x] Create project root `package.json` with scripts
- [x] Set up frontend — Vite + React + TypeScript scaffold
  - [x] `npm create vite@latest . -- --template react-ts`
  - [x] Configure `tsconfig.json` with strict mode
  - [x] Configure `vite.config.ts` with API proxy for local dev
- [x] Set up API project — Azure Functions v4 with TypeScript
  - [x] Create `api/` directory with `package.json`
  - [x] Configure `api/tsconfig.json`
  - [x] Create `api/host.json` and `api/local.settings.json`
  - [x] Add `@azure/functions` and `@azure/cosmos` dependencies
- [x] Create `staticwebapp.config.json` with auth routes and navigation fallback
- [x] Install and configure SWA CLI for local development
- [x] Set up linting — ESLint + Prettier for both frontend and API
- [x] Create `README.md` with project overview and local dev instructions

## Phase 1: Azure Infrastructure

- [ ] Create Azure Resource Group — `rg-whiskyapp-dev`
- [ ] Provision Azure Cosmos DB account — serverless, NoSQL API
  - [ ] Create database `whiskyapp`
  - [ ] Create container `events` — partition key `/id`
  - [ ] Create container `whiskeys` — partition key `/eventId`
  - [ ] Create container `ratings` — partition key `/whiskeyId`
- [ ] Provision Azure Static Web Apps resource
  - [ ] Connect to GitHub repository
  - [ ] Configure build settings — app: `/`, api: `api`, output: `dist`
- [ ] Set up Google OAuth credentials in Google Cloud Console
  - [ ] Create OAuth 2.0 client ID — web application
  - [ ] Configure authorized redirect URI
- [ ] Configure SWA application settings
  - [ ] Set `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`
  - [ ] Set `COSMOS_CONNECTION_STRING` and `COSMOS_DATABASE_NAME`
- [x] Set up GitHub Actions workflow — auto-generated by SWA
- [ ] Verify deployment pipeline — push to main triggers build and deploy

## Phase 2: Backend API — Core

- [x] Implement Cosmos DB client singleton — `api/src/lib/cosmos.ts`
- [x] Implement auth middleware — `api/src/lib/auth.ts`
  - [x] `getClientPrincipal()` — extract user from SWA header
  - [x] `isAuthenticated()` — check authenticated role
  - [x] `isAdmin()` — check admin role
- [x] Implement response helpers — `api/src/lib/response.ts`
- [x] Define TypeScript models — `frontend/src/types/index.ts`
  - [x] Event model
  - [x] Whiskey model
  - [x] Rating model
  - [x] User / ClientPrincipal model

## Phase 3: Backend API — Events

- [x] Implement Event API functions — `api/src/functions/events.ts`
  - [x] `GET /api/events` — list events
  - [x] `POST /api/events` — create event — admin only
  - [x] `GET /api/events/:eventId` — get event with whiskeys
  - [x] `PUT /api/events/:eventId` — update event — admin only
  - [x] `DELETE /api/events/:eventId` — delete event — admin only
- [ ] Write unit tests for Event service
- [ ] Write integration tests for Event API functions

## Phase 4: Backend API — Whiskeys

- [x] Implement Whiskey API functions — `api/src/functions/whiskeys.ts`
  - [x] `GET /api/events/:eventId/whiskeys` — list whiskeys
  - [x] `POST /api/events/:eventId/whiskeys` — add whiskey — admin only
  - [x] `DELETE /api/events/:eventId/whiskeys/:whiskeyId` — remove whiskey — admin only
- [ ] Write unit tests for Whiskey service
- [ ] Write integration tests for Whiskey API functions

## Phase 5: Backend API — Ratings

- [x] Implement Rating API functions — `api/src/functions/ratings.ts`
  - [x] `PUT /api/events/:eventId/whiskeys/:whiskeyId/rating` — upsert rating
  - [x] `DELETE /api/events/:eventId/whiskeys/:whiskeyId/rating` — remove rating
- [ ] Write unit tests for Rating service
- [ ] Write integration tests for Rating API functions

## Phase 6: Frontend — Foundation

- [x] Set up project structure — `src/components/`, `src/pages/`, `src/hooks/`, `src/api/`, `src/types/`
- [x] Define TypeScript types — `src/types/index.ts`
  - [x] Event types
  - [x] Whiskey types
  - [x] Rating types
  - [x] API response types
- [x] Implement API client — `src/api/client.ts`
  - [x] Events API functions — `src/api/events.ts`
  - [x] Whiskeys API functions — `src/api/whiskeys.ts`
  - [x] Ratings API functions — `src/api/ratings.ts`
- [x] Implement auth hook — `src/hooks/useAuth.ts`
  - [x] Fetch `/.auth/me` on mount
  - [x] Provide user context — `useAuth()` hook
  - [x] Login/logout navigation helpers
- [x] Set up React Router with route definitions
- [x] Create Layout component with Header — `src/components/Layout.tsx`
  - [x] Logo and app name
  - [x] Navigation links
  - [x] AuthButton — sign in / sign out / user display

## Phase 7: Frontend — Event Pages

- [x] Implement EventsPage — `src/pages/EventsPage.tsx`
  - [x] Fetch and display events using `useEvents()` hook
  - [x] Event list with links to detail
  - [x] Loading and error states
- [x] Implement EventDetailPage — `src/pages/EventDetailPage.tsx`
  - [x] Fetch event and whiskeys using hooks
  - [x] Event header with details
  - [x] Loading and error states
  - [x] 404 handling for invalid event ID

## Phase 8: Frontend — Whiskey List and Ratings

- [x] Implement WhiskeyDetailPage — `src/pages/WhiskeyDetailPage.tsx`
  - [x] Display whiskey details
  - [x] Rating interaction
- [x] Implement custom hooks
  - [x] `useEvents()` — `src/hooks/useEvents.ts`
  - [x] `useWhiskeys()` — `src/hooks/useWhiskeys.ts`
  - [x] `useRatings()` — `src/hooks/useRatings.ts`

## Phase 9: Styling and UX Polish

- [x] CSS Modules approach — component-scoped styles
- [x] Style Layout and navigation — `Layout.module.css`
- [x] Style EventsPage — `EventsPage.module.css`
- [x] Style EventDetailPage — `EventDetailPage.module.css`
- [x] Style WhiskeyDetailPage — `WhiskeyDetailPage.module.css`
- [x] Style NotFoundPage — `NotFoundPage.module.css`
- [ ] Add loading skeletons or spinners
- [ ] Add toast notifications for success/error feedback
- [ ] Ensure WCAG 2.1 AA accessibility basics — focus management, ARIA labels, color contrast

## Phase 10: Testing

- [ ] Frontend unit tests — utility functions, hooks
- [ ] Frontend component tests — key components with React Testing Library
- [ ] Frontend integration tests — page flows with MSW mocked API
- [ ] Backend unit tests — all service functions
- [ ] Backend integration tests — all API endpoints
- [ ] End-to-end tests with Playwright
  - [ ] Anonymous user — browse events and whiskeys
  - [ ] Authenticated user — sign in, rate whiskey, update rating, remove rating
  - [ ] Admin user — create event, add whiskey, delete whiskey, delete event
- [ ] Manual testing on mobile devices

## Phase 11: Production Readiness

- [x] Review and harden `staticwebapp.config.json` auth routes
- [x] Verify all API endpoints enforce proper authorization
- [ ] Add input sanitization review
- [ ] Set up production Azure resource group — `rg-whiskyapp-prod`
- [ ] Provision production Cosmos DB and SWA resources
- [ ] Configure production environment variables
- [ ] Configure custom domain — if desired
- [ ] Set up Azure Monitor / Application Insights — optional for MVP
- [ ] Final deployment and smoke test
- [ ] Assign admin roles in production SWA
- [x] Write deployment documentation — `context/WhiskyApp/deployment.md`

## Phase 12: Operational Tasks (Post-Deployment)

- [ ] Enable Application Insights for monitoring
- [ ] Set up alerts for error rates and latency
- [ ] Configure Cosmos DB backup policy review
- [ ] Document runbook for common operational tasks
- [ ] Review and rotate Cosmos DB keys periodically
- [ ] Monitor Cosmos DB RU consumption and optimize queries if needed
- [ ] Review Google OAuth consent screen — publish app if needed for broader access
- [ ] Set up cost alerts in Azure to monitor spending
